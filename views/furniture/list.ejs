<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Furniture List</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link rel="stylesheet"
        href="https://demos.creative-tim.com/notus-js/assets/vendor/@fortawesome/fontawesome-free/css/all.min.css">
    <style>
        html, body {
    height: 100%;
    margin: 0;
    padding: 0;
}
body {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}
main, .modern-container {
    flex: 1 0 auto;
}
footer {
    flex-shrink: 0;
}
        .modern-container {
            max-width: 1200px;
            margin: 48px auto 0 auto;
            padding: 2.5rem 2rem 2rem 2rem;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44,62,80,0.10);
            border: 1px solid #e3e6ed;
            animation: fadeIn 0.5s;
        }
        .modern-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2375D8;
            text-align: center;
            margin-bottom: 2rem;
            letter-spacing: 1px;
        }
        .modern-btn-primary {
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
            font-weight: 700;
            font-size: 1.1rem;
            padding: 0.7rem 1.5rem;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 16px rgba(36,117,216,0.10);
            transition: background 0.2s, transform 0.15s;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 0.7rem;
            text-decoration: none;
        }
        .modern-btn-primary:hover {
            background: linear-gradient(90deg, #0c9083 0%, #2375D8 100%);
            transform: translateY(-2px) scale(1.03);
            box-shadow: 0 8px 24px rgba(36,117,216,0.18);
        }
        .modern-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
            background: #f9fafb;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(44,62,80,0.06);
        }
        .modern-table th, .modern-table td {
            padding: 1rem 1.2rem;
            text-align: left;
        }
        .modern-table th {
            background: #2375D8;
            color: #fff;
            font-weight: 600;
            font-size: 1rem;
            border-bottom: 2px solid #e3e6ed;
        }
        .modern-table tbody tr {
            background: #fff;
            transition: background 0.2s;
        }
        .modern-table tbody tr:hover {
            background: #e6f0fa;
        }
        .modern-table td {
            border-bottom: 1px solid #e3e6ed;
            font-size: 1rem;
            color: #374151;
        }
        .modern-table .actions a, .modern-table .actions button {
            color: #2375D8;
            margin-right: 0.5rem;
            font-size: 1.1rem;
            transition: color 0.2s;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0 0.3rem;
        }
        .modern-table .actions a:hover, .modern-table .actions button:hover {
            color: #0c9083;
        }
        .modern-table .actions button.delete {
            color: #ef4444;
        }
        .modern-table .actions button.delete:hover {
            color: #b91c1c;
        }
        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.5);
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(4px);
            animation: fadeInModal 0.3s;
        }
        .modal-content {
            background-color: #fff;
            margin: auto;
            padding: 2.5rem 2rem;
            border-radius: 16px;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            width: 90%;
            max-width: 420px;
            text-align: center;
            position: relative;
            animation: slideInModal 0.3s;
        }
        .modal-content h3 {
            color: #1f2937;
            margin-top: 0;
            margin-bottom: 0.5rem;
            font-size: 1.5rem;
            font-weight: 700;
        }
        .modal-content p {
            color: #6b7280;
            margin-bottom: 2rem;
            line-height: 1.6;
            font-size: 1rem;
        }
        .modal-buttons {
            display: flex;
            justify-content: center;
            gap: 1rem;
        }
        .btn-cancel, .btn-confirm {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 1rem;
        }
        .btn-cancel {
            background-color: #f3f4f6;
            color: #4b5563;
            border: 1px solid #e5e7eb;
        }
        .btn-cancel:hover {
            background-color: #e5e7eb;
            color: #1f2937;
        }
        .btn-confirm {
            background-color: #ef4444;
            color: white;
            box-shadow: 0 4px 6px -1px rgba(239, 68, 68, 0.3);
        }
        .btn-confirm:hover {
            background-color: #dc2626;
            transform: translateY(-1px);
            box-shadow: 0 6px 8px -1px rgba(239, 68, 68, 0.4);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(18px);}
            to { opacity: 1; transform: translateY(0);}
        }
        @keyframes fadeInModal {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes slideInModal {
            from { transform: translateY(20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
    </style>
</head>

<body>
   <%- include('../shared/header') %>
 
    <main>
        <div class="modern-container">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
                <div class="modern-title">
                    <i class="fas fa-couch"></i> Furniture List
                </div>
                <a href="/furniture/add" class="modern-btn-primary">
                    <i class="fas fa-plus"></i> Add New Furniture
                </a>
            </div>
             <% if (message) { %>
                <div class="message <%= messageType %>">
                    <%= message %>
                </div>
            <% } %>
            <div class="overflow-x-auto">
                <table class="modern-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Description</th>
                            <th>Created At</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (furniture && furniture.length > 0) { %>
                            <% furniture.forEach(item => { %>
                                <tr>
                                    <td><strong><%= item.id %></strong></td>
                                    <td><%= item.name %></td>
                                    <td><%= item.description || 'No description' %></td>
                                    <td><%= new Date(item.created_at).toLocaleDateString('vi-VN') %></td>
                                    <td class="actions">
                                        <a href="/furniture/edit/<%= item.id %>" title="Edit"><i class="fas fa-edit"></i></a>
                                        <button title="Delete" class="delete" onclick="showDeleteModal(<%= item.id %>, '<%= item.name %>')"><i class="fas fa-trash-alt"></i></button>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" style="text-align:center; color:#888; padding:2rem;">No furniture found</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <div id="deleteModal" class="modal">
        <div class="modal-content">
            <div style="color: #ef4444; font-size: 3.5rem; margin-bottom: 1rem; opacity: 0.9;">
                <i class="fas fa-exclamation-circle"></i>
            </div>
            <h3>Confirm Deletion</h3>
            <p>Are you sure you want to delete this furniture item?<br><strong id="furnitureName" style="color: #1f2937; display: block; margin-top: 0.5rem;"></strong></p>
            <div class="modal-buttons">
                <button class="btn-cancel" onclick="closeDeleteModal()">Cancel</button>
                <form id="deleteForm" method="POST" style="display:inline;">
                    <button type="submit" class="btn-confirm">Delete</button>
                </form>
            </div>
        </div>
    </div>
    
    <%- include('../shared/footer.ejs') %>

    <script>
        function showDeleteModal(id, name) {
            const deleteForm = document.getElementById('deleteForm');
            deleteForm.action = `/furniture/delete/${id}?_method=POST`;
            document.getElementById('furnitureName').innerText = `Name: ${name}`;
            document.getElementById('deleteModal').style.display = 'flex';
        }

        function closeDeleteModal() {
            document.getElementById('deleteModal').style.display = 'none';
        }

        window.onclick = function(event) {
            const modal = document.getElementById('deleteModal');
            if (event.target == modal) {
                closeDeleteModal();
            }
        }
    </script>
</body>

</html>
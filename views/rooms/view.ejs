<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room Details</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <style>
        .modern-container {
            max-width: 1000px;
            margin: 48px auto;
            padding: 2.5rem;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44, 62, 80, 0.10);
            animation: fadeIn 0.5s;
        }

        .modern-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2375D8;
            margin-bottom: 2rem;
        }

        .modern-section {
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: #f7fafc;
            border-radius: 10px;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1.5rem;
        }
        
        .info-item {
            display: flex;
            flex-direction: column;
        }

        .info-label {
            font-size: 0.875rem;
            color: #718096;
        }

        .info-value {
            font-size: 1.1rem;
            color: #2d3748;
            font-weight: 600;
        }

        .modern-btn-primary {
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
            font-weight: 700;
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 10px;
            text-decoration: none;
        }
        
        .modern-btn-secondary {
            background-color: #e2e8f0;
            color: #4a5568;
            font-weight: 600;
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 10px;
            text-decoration: none;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(18px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
    <%- include('../shared/header.ejs') %>

    <div class="modern-container">
        <div style="display: flex; justify-content: space-between; align-items: center;">
            <h1 class="modern-title">🏠 <%= room.room_number %></h1>
            <div>
                <a href="/rooms/edit/<%= room.id %>" class="modern-btn-primary">✏️ Edit</a>
                <a href="/rooms/list" class="modern-btn-secondary">← Back to List</a>
            </div>
        </div>

        <div class="modern-section">
            <h2 style="font-size: 1.5rem; font-weight: 600; margin-bottom: 1rem;">ℹ️ Room Information</h2>
            <div class="info-grid">
                <div class="info-item">
                    <span class="info-label">ID</span>
                    <span class="info-value"><%= room.id %></span>
                </div>
                <div class="info-item">
                    <span class="info-label">Room Number</span>
                    <span class="info-value"><%= room.room_number %></span>
                </div>
                <div class="info-item">
                    <span class="info-label">Type</span>
                    <span class="info-value"><%= room.room_type %></span>
                </div>
                <div class="info-item">
                    <span class="info-label">Size</span>
                    <span class="info-value"><%= room.size_sqm ? room.size_sqm + ' m²' : 'N/A' %></span>
                </div>
                <div class="info-item">
                    <span class="info-label">Rent Price</span>
                    <span class="info-value" style="color: #28a745;"><%= parseFloat(room.rent_price).toLocaleString('vi-VN') %> VND</span>
                </div>
            </div>
            <% if (room.description) { %>
                <div style="margin-top: 1.5rem;">
                    <span class="info-label">Description</span>
                    <p><%= room.description %></p>
                </div>
            <% } %>
        </div>

        <div class="modern-section">
            <h2 style="font-size: 1.5rem; font-weight: 600; margin-bottom: 1rem;">🪑 Furniture</h2>
            <% if (furnitureList && furnitureList.length > 0) { %>
                <div class="info-grid">
                    <% furnitureList.forEach(item => { %>
                        <div class="info-item" style="background: #f0f4f8; padding: 1rem; border-radius: 8px;">
                            <span class="info-value">📦 <%= item.name %></span>
                            <span class="info-label">Quantity: <%= item.quantity %></span>
                            <% if (item.description) { %>
                                <p style="font-size: 0.8rem; color: #666;"><%= item.description %></p>
                            <% } %>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p>No furniture assigned to this room.</p>
            <% } %>
        </div>
    </div>

    <%- include('../shared/footer.ejs') %>
</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Cash Payments</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <style>
        .modern-container {
            max-width: 1200px;
            margin: 48px auto;
            padding: 2.5rem;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44, 62, 80, 0.10);
            animation: fadeIn 0.5s;
        }

        .modern-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2375D8;
            margin-bottom: 2rem;
        }

        .modern-table {
            width: 100%;
            border-collapse: collapse;
        }
        .modern-table th, .modern-table td {
            padding: 1rem 1.2rem;
            text-align: left;
        }
        .modern-table th {
            background: #2375D8;
            color: #fff;
            font-weight: 600;
        }
        .modern-table tbody tr:hover {
            background: #e6f0fa;
        }
        .modern-table td {
            border-bottom: 1px solid #e3e6ed;
        }
        
        .modern-btn-primary {
            background-color: #10b981;
            color: #fff;
            font-weight: 700;
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 10px;
            cursor: pointer;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(18px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
    <%- include('../shared/header.ejs') %>

    <div class="modern-container">
        <h1 class="modern-title"><i class="fas fa-hourglass-half"></i> Pending Cash Payments</h1>

        <% if (messages.success) { %>
        <div class="alert alert-success"><%= messages.success %></div>
        <% } %>
        <% if (messages.error) { %>
        <div class="alert alert-danger"><%= messages.error %></div>
        <% } %>

        <% if (payments && payments.length > 0) { %>
        <div style="overflow-x: auto;">
            <table class="modern-table">
                <thead>
                    <tr>
                        <th>Payment ID</th>
                        <th>Customer</th>
                        <th>Room</th>
                        <th>Amount</th>
                        <th>Date</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% payments.forEach(payment => { %>
                    <tr>
                        <td>#<%= payment.payment_id %></td>
                        <td><%= payment.full_name %></td>
                        <td><%= payment.room_number %></td>
                        <td><%= new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(payment.amount) %></td>
                        <td><%= new Date(payment.payment_date).toLocaleString() %></td>
                        <td>
                            <form action="/renting/confirm-payment/<%= payment.payment_id %>" method="POST"
                                onsubmit="return confirm('Are you sure you want to confirm this payment?');">
                                <button type="submit" class="modern-btn-primary">Confirm Payment</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <% } else { %>
        <div class="text-center p-5 border rounded bg-light">
            <h4>No Pending Payments</h4>
            <p>There are currently no pending cash payments to confirm.</p>
        </div>
        <% } %>
    </div>
    
    <%- include('../shared/footer.ejs') %>

</body>

</html>
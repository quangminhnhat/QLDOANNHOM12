<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Furniture</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            padding: 0;
        }

        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: #f8f9fa;
        }

        main,
        .modern-container {
            flex: 1 0 auto;
        }

        footer {
            flex-shrink: 0;
        }

        .modern-container {
            max-width: 900px;
            margin: 48px auto 0 auto;
            padding: 2.5rem;
            background: #ffffff;
            border-radius: 24px;
            box-shadow: 0 8px 32px rgba(35, 117, 216, 0.1);
            border: 1px solid #dbeeff;
            animation: fadeIn 0.5s;
        }

        .modern-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2375D8;
            text-align: center;
            margin-bottom: 2rem;
            letter-spacing: 1px;
        }

        .modern-form-group {
            margin-bottom: 1.5rem;
        }

        .modern-form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #4a5568;
        }

        .modern-form-control {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        .modern-form-control:focus {
            outline: none;
            border-color: #2375D8;
            box-shadow: 0 0 0 4px rgba(35, 117, 216, 0.1);
        }

        .modern-btn-primary {
            width: 100%;
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
            font-weight: 700;
            font-size: 1.1rem;
            padding: 0.85rem 0;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 16px rgba(36, 117, 216, 0.1);
            transition: background 0.2s, transform 0.15s;
            cursor: pointer;
            margin-top: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.7rem;
        }

        .modern-btn-primary:hover {
            background: linear-gradient(90deg, #0c9083 0%, #2375D8 100%);
            transform: translateY(-2px) scale(1.03);
            box-shadow: 0 8px 24px rgba(36, 117, 216, 0.18);
        }

        .modern-btn-secondary {
            width: 100%;
            background: #e2e8f0;
            color: #4a5568;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 0.85rem 0;
            border: none;
            border-radius: 10px;
            transition: background 0.2s, color 0.2s;
            cursor: pointer;
            margin-top: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.7rem;
            text-decoration: none;
            text-align: center;
        }

        .modern-btn-secondary:hover {
            background: #cbd5e0;
            color: #2375D8;
        }
        
        .modern-form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(18px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <%- include('../shared/header') %>

    <main>
        <div class="modern-container">
            <div class="modern-title">
                <i class="fas fa-edit"></i> Edit Furniture
            </div>

            <% if (message) { %>
            <div class="alert alert-danger">
                <%= message %>
            </div>
            <% } %>

            <form action="/furniture/edit/<%= furniture.id %>" method="POST">
                <div class="modern-form-group">
                    <label for="name">Furniture Name</label>
                    <input type="text" class="modern-form-control" id="name" name="name" value="<%= furniture.name %>" required maxlength="100">
                </div>
                
                <div class="modern-form-group">
                    <label for="description">Description</label>
                    <textarea class="modern-form-control" id="description" name="description" rows="4" maxlength="255"><%= furniture.description || '' %></textarea>
                </div>


                <div style="display: flex; gap: 1rem; margin-top: 2rem;">
                    <a href="/furniture/list" class="modern-btn-secondary">
                        <i class="fas fa-arrow-left"></i> Cancel
                    </a>
                    <button type="submit" class="modern-btn-primary">
                        <i class="fas fa-save"></i> Save Changes
                    </button>
                </div>
            </form>
        </div>
    </main>

    <%- include('../shared/footer.ejs') %>

</body>

</html>
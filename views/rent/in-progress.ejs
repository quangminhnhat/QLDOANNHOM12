<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>In-Progress Rentals</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <style>
        .modern-container {
            max-width: 1200px;
            margin: 48px auto;
            padding: 2.5rem;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44, 62, 80, 0.10);
            animation: fadeIn 0.5s;
        }

        .modern-title {
            font-size: 2rem;
            font-weight: 700;
            color: #2375D8;
            margin-bottom: 2rem;
        }
        
        .modern-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
            background: #f9fafb;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(44,62,80,0.06);
        }
        .modern-table th, .modern-table td {
            padding: 1rem 1.2rem;
            text-align: left;
        }
        .modern-table th {
            background: #2375D8;
            color: #fff;
            font-weight: 600;
        }
        .modern-table tbody tr:hover {
            background: #e6f0fa;
        }
        .modern-table td {
            border-bottom: 1px solid #e3e6ed;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(18px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
    <%- include('../shared/header.ejs') %>

    <div class="modern-container">
        <h1 class="modern-title"><i class="fas fa-tasks"></i> In-Progress Rentals</h1>

        <% if (messages.success) { %>
            <div class="alert alert-success"><%= messages.success %></div>
        <% } %>
        <% if (messages.error) { %>
            <div class="alert alert-danger"><%= messages.error %></div>
        <% } %>

        <% if (rentals && rentals.length > 0) { %>
            <div style="overflow-x: auto;">
                <table class="modern-table">
                    <thead>
                        <tr>
                            <th>Contract ID</th>
                            <th>Customer</th>
                            <th>Room</th>
                            <th>Start Date</th>
                            <th>End Date</th>
                            <th>Total Rent</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% rentals.forEach(rental => { %>
                            <tr>
                                <td>#<%= rental.contract_id %></td>
                                <td><%= rental.full_name %></td>
                                <td><%= rental.room_number %></td>
                                <td><%= new Date(rental.start_date).toLocaleDateString() %></td>
                                <td><%= new Date(rental.end_date).toLocaleDateString() %></td>
                                <td><%= new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(rental.total_rent) %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="text-center p-5 border rounded bg-light">
                <h4>No Rentals In Progress</h4>
                <p>There are currently no active rentals in progress.</p>
            </div>
        <% } %>
    </div>
    
    <%- include('../shared/footer.ejs') %>

</body>
</html>
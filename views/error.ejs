<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/styles/tailwind.css">
    <style>
        .modern-container {
            max-width: 800px;
            margin: 48px auto;
            padding: 2.5rem;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(44, 62, 80, 0.10);
            animation: fadeIn 0.5s;
            text-align: center;
        }

        .error-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #ef4444;
            margin-bottom: 1rem;
        }

        .error-message {
            font-size: 1.25rem;
            color: #4a5568;
            margin-bottom: 2rem;
        }

        .modern-btn-primary {
            background: linear-gradient(90deg, #2375D8 0%, #0c9083 100%);
            color: #fff;
            font-weight: 700;
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 10px;
            text-decoration: none;
            margin: 0 0.5rem;
        }
        
        .modern-btn-secondary {
            background-color: #e2e8f0;
            color: #4a5568;
            font-weight: 600;
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 10px;
            text-decoration: none;
            margin: 0 0.5rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(18px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>

<body>
    <%- include('./shared/header.ejs') %>

    <div class="modern-container">
        <div style="font-size: 4rem; color: #ef4444;"><i class="fas fa-exclamation-triangle"></i></div>
        <h1 class="error-title">An Error Occurred</h1>
        <p class="error-message"><%= message %></p>
        <div>
            <a href="javascript:history.back()" class="modern-btn-secondary">Go Back</a>
            <a href="/" class="modern-btn-primary">Go Home</a>
        </div>
    </div>

    <%- include('./shared/footer.ejs') %>
</body>

</html>